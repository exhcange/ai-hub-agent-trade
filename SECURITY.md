# Security policy

Never report API keys, secret keys, credential-manager contents, or local profile files through issues.

Report a vulnerability privately to the AI Hub security contact before publishing details. Release credentials must be revoked and replaced if they are exposed in a ticket, log, terminal history, or source control.

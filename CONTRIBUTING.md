# Contributing

Use Node.js 20 or newer and pnpm 9 or newer.

Run `pnpm release-check` before submitting changes. New API capabilities need a Core test and must be registered through the shared Tool Registry path; do not add direct HTTP calls in CLI or MCP packages.

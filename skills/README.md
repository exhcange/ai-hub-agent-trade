# Skills

The initial release contains six English-only spot Skills. Each Skill is added when the corresponding CLI commands and MCP Tools are available and tested. Skills must use `ai-hub` commands only and must require the shared prepare/confirm workflow for state-changing actions.
